# Bill of materials

Two BOM copies are provided:

- `AT-DX1_BOM_RP2354A_2026-09-14.csv` — normalized UTF-8, comma-separated copy for convenient viewing on GitHub and use with standard tools.
- `source/BOM_FT8-FT4_RP2354A_2026-09-14_EasyEDA-export.csv` — original supplied EasyEDA export retained unchanged. Despite the `.csv` extension, the original file is UTF-16 and tab-delimited.

The normalized file preserves the same rows and component data; only text encoding and field delimiter were changed.
